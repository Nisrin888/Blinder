services:
  db:
    image: pgvector/pgvector:pg16
    container_name: blinder-db
    environment:
      POSTGRES_USER: blinder
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: blinder_mvp
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
